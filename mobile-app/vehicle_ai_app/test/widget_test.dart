import 'package:flutter_test/flutter_test.dart';
import 'package:vehicle_ai_app/main.dart'; // ← replace with your actual package name

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Nothing to test yet — placeholder
  });
}
